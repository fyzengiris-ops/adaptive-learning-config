# 变更范围：通用知识树-详情顶栏按钮-知识卡片连带

## 批次信息

- 批次 ID：`kt-detail-topbar-knowledge-card-20260825`
- 关联页面/流程：通用知识树 · 学科详情页 · 顶栏操作按钮
- 路由与主要文件：`/knowledge-system` · `src/app/system-settings/KnowledgeTree.tsx`
- 审核模式：增量
- 关联清单：`docs/prd-workflow/inventories/knowledge-tree-detail-topbar-knowledge-card.inventory.md`

## 本次变更摘要

知识卡片业务逻辑落地后，完善详情顶栏按钮与知识卡片的耦合：导出/导入是否承载知识卡片；保存编辑按已拍板口径合并实现（不重审、校验失败不切学习资源 Tab）。

## A 直接变更（预期）

| 对象 | 说明 |
| --- | --- |
| 无独立新控件 | 本批不新增顶栏按钮，只补既有按钮对知识卡片的口径 |

## B 连带影响（预期）

| 对象 | 可能被影响的原因 |
| --- | --- |
| 导出知识树 | Excel 现无知识卡片列，需定是否导出、说明是否写明 |
| 批量导入 | 模板现无知识卡片列，需定是否写入要点/模块/例题；清关联考点清卡片内容已拍板 |

## C 明确不重审

| 对象或区域 | 原因 |
| --- | --- |
| 保存编辑主流程 | 空行剔除、模块名校验、学习资源变更维度已拍板；不自动切学习资源 Tab |
| 编辑知识点详情 | 入口与回显已审 |
| 取消 | 未保存确认已审 |
| 导出/导入已有列（学业要求、考频、考点、前置、策略） | 前几轮已审 |

## 约束

- 仅对 A∪B 做后续 Skill1 审核与 logicSections / PRD 增量。
- 不对 C 重审或改写既有业务口径。
- 仅对 A∪B 审核与编写业务逻辑；不对 C 重审或改写既有口径。
