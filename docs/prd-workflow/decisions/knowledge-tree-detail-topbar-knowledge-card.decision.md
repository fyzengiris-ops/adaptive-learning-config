# 通用知识树·详情顶栏按钮（知识卡片连带）决策记录

## 页面范围

- 页面/流程：通用知识树 · 学科详情页 · 顶栏操作按钮（相对知识卡片的连带口径）
- 路由：`/knowledge-system`（Tab：通用知识树）
- 相关代码文件：
  - `src/app/system-settings/KnowledgeTree.tsx`
  - `src/components/shared/KnowledgeCardPanel.tsx`
  - `src/components/shared/ImportValidationModal.tsx`
- 审核模式：增量
- 本次变更摘要：知识卡片业务逻辑落地后，完善详情顶栏导出/导入与知识卡片的耦合；保存编辑按已拍板口径合并，不重审
- 关联清单：`docs/prd-workflow/inventories/knowledge-tree-detail-topbar-knowledge-card.inventory.md`
- 关联 scope：`docs/prd-workflow/change-scopes/kt-detail-topbar-knowledge-card-20260825.scope.md`

## 已确认决策

1. 按钮：导出知识树
   - 影响类型：B
   - 决策：自定义方案（基于 A，不要说明文案）
   - 结论：知识卡片（核心概念、考点说明、典型例题）不进入导出文件。导出顶行说明不增加知识卡片相关文案；已有列（层级标题、学业要求、考频、考点名称/类型、前置知识点、出题策略）与现有说明维持现状。
   - 影响范围：导出知识树 Excel 列与顶行说明

2. 按钮：批量导入
   - 影响类型：B
   - 决策：自定义方案（基于 A，不要说明文案）
   - 结论：导入不写入知识卡片正文（要点、自定义模块、考点说明、典型例题）。导入说明、模板顶行说明均不增加知识卡片相关文案。已确认的「考点名称填【清空】时一并清除知识卡片对应考点内容」保持不变。
   - 影响范围：批量导入模板、校验、写回、导入弹窗说明

## 明确不重审（增量 C 类）

- 保存编辑主流程 — 空要点/空条目保存后去掉、模块空名/重名拦截、变更维度「学习资源」、例题选填不拦截，已在学习资源·知识卡片批次拍板。本轮不重审。校验失败停留当前 Tab，不自动切到学习资源。
- 编辑知识点详情 — 入口/权限/进编辑时装载暂存已审。
- 取消 — 未保存确认已审；取消丢弃含知识卡片在内的暂存。
- 导出/导入已有列（学业要求、考频、考点、前置、策略）及既有校验 — 前几轮已审。

## 暂不处理

- 无

## 待后续确认

- 无

## Skill2 必写提醒

- 导出、导入注册表增量写入「知识卡片不进表、说明不提及」。
- 保存编辑：合并学业要求 / 关联考点 / 知识卡片已拍板口径为顶栏单角标；实现模块空名/重名拦截与空行剔除；失败不切 Tab。
